OPENAI_API_KEY=sk-...
# Set to "true" to enable LLM refinement, otherwise outputs are template-only
MCP_USE_LLM=false
MCP_OPENAI_MODEL=gpt-4o-mini
# Optional: custom Drive folder name for outputs
MCP_OUTPUT_FOLDER=MCP Agent Outputs
